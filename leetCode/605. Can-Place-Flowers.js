// You have a long flowerbed in which some of the plots are planted, and some are not. However, flowers cannot be planted in adjacent plots.

// Given an integer array flowerbed containing 0's and 1's, where 0 means empty and 1 means not empty, and an integer n, return true if n new flowers can be planted in the flowerbed without violating the no-adjacent-flowers rule and false otherwise.

// flowerbed = [1,0,0,0,1], n = 1
// Output: true

// flowerbed = [1,0,0,0,1], n = 1
// Output: false

var canPlaceFlowers = function (flowerbed, n) {
  if (n === 0) {
    return true;
  }

  let flowerCount = 0;
  for (let i = 0; i < flowerbed.length; i++) {
    let leftEmpty = i === 0 || flowerbed[i - 1] === 0;
    let rightEmpty = i === flowerbed.length - 1 || flowerbed[i + 1] === 0;

    if (flowerbed[i] === 0 && leftEmpty && rightEmpty) {
      flowerCount++;
      flowerbed[i] = 1;
      if (flowerCount >= n) {
        return true;
      }
    }
  }

  return false;
};

console.log(canPlaceFlowers([1, 0, 0, 0, 1], 1)); // true
console.log(canPlaceFlowers([1, 0, 0, 0, 1], 2)); // false


// Подсказки, которые подскажут тебе как подойти к решению, если не получается стартануть

// Чтобы решить задачу, необходимо пройти через массив flowerbed и определить, можно ли посадить новые цветы, не нарушая правила соседних цветов. Вот пошаговое руководство с подсказками:

// Подсказка 1: Определение правил посадки
// Цветы не могут быть посажены в соседние участки.
// Если участок свободен (0) и его соседние участки (слева и справа) также свободны или выходят за границы массива, то можно посадить цветок в этот участок.
// Подсказка 2: Итерация через массив
// Итерация через массив flowerbed поможет определить, куда можно посадить цветы. Подумайте о том, как вы можете проверить текущий и соседние участки.

// Подсказка 3: Условия для посадки
// Проверяйте следующие условия для каждого участка:

// Текущий участок свободен (0).
// Левый соседний участок либо свободен (0), либо это начало массива.
// Правый соседний участок либо свободен (0), либо это конец массива.
// Подсказка 4: Обновление массива и счетчика
// Если вы нашли место для посадки цветка, обновите массив, пометив участок как занятый (1), и увеличьте счетчик посаженных цветов.

// Подсказка 5: Завершение проверки
// Если вы достигли числа n посаженных цветов, вы можете сразу вернуть true. Если итерация завершилась, а посаженных цветов меньше n, верните false.

// Псевдокод
// Попробуйте написать псевдокод на основе этих подсказок:

// Инициализируйте счетчик посаженных цветов count = 0.
// Пройдите через массив flowerbed с помощью цикла.
// Для каждого участка проверьте условия посадки.
// Если можно посадить цветок, обновите массив и увеличьте счетчик count.
// Если count становится равным n, верните true.
// Если цикл завершен, верните false.
// Реализация
// Попробуйте реализовать решение на основе псевдокода. Если у вас возникнут трудности, дайте знать, и я помогу с дополнительными подсказками!
